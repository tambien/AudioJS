"use strict";

var AUDIO = {
	/* 
		@const 
	*/
	version : "0.0.2",
	/* the audio context */
	context : new webkitAudioContext(),
	/* 
		set the bpm 
		@param {number} bpm
	*/
	setTempo : function( bpm ){
		// this.METRO.setTempo(bpm);
	}
}
