/*=============================================================================
	TRANSPORT

	keeps track of the duration of the 
=============================================================================*/

AUDIO.TRANS = {
	/* @private */
	currentTime : 0,
	/* @private */
	state : 'READY',
	/* start / restart the transport */
	start : function(){
		this.state = started
	},
	/* stop the metro */
	stop : function(){

	},
	/* pause it */
	pause : function(){

	}
}